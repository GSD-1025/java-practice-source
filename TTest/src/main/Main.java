package main;

import manage.userManager;

public class Main {

	public static void main(String[] args) {
		new userManager();
		//Main 클래스에서 userManager클래스를 의존한다.
		//의존을 객체를 직접 만들어서 해결한다.
		//객체를 만들때 생성자로 객페를 만든다.
		//생성자를 call하다, 즉 접근한다.
		
		
	}

}
